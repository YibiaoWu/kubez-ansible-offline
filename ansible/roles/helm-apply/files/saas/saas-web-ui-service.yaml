apiVersion: v1
kind: Service
metadata:
  name: saas-web-ui
  namespace: saas
  labels:
    app: saas
spec:
  type: ClusterIP
  sessionAffinity: ClientIP
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  selector:
    app: saas
    component: saas-web-ui