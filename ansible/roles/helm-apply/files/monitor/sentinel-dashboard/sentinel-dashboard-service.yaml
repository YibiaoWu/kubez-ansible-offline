apiVersion: v1
kind: Service
metadata:
  name: sentinel-dashboard
  namespace: monitor
  labels:
    app: monitor
spec:
  type: NodePort
  ports:
    - protocol: TCP
      port: 8081
      targetPort: 8080
      nodePort: 48081
  selector:
    app: monitor
    component: sentinel-dashboard
